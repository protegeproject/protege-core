<?xml version = "1.0" encoding = "UTF-8"?>
<project name = "protege" default = "usage" basedir = ".">
	<property file = "../global.properties"/>
	<property name = "project.name" value = "protege-core"/>
	<property name = "build.dir" value = "./build"/>
	<property name = "build.dest" value = "${build.dir}/classes"/>
	<property name = "build.src" value = "${build.dir}/src"/>
	<property name = "docs.dir" value = "./docs"/>
	<property name = "build.javadoc" value = "${docs.dir}/apidocs"/>
	<property name = "build.scripts" value = "./bin"/>
	<property name = "dist.dir" value = "./dist"/>
	<property name = "lib.dir" value = "./lib"/>
	<property name = "deprecation" value = "on"/>    
	<property name = "debug" value = "off"/>
	<property name = "includeAntRuntime" value = "no"/>
	<property name = "optimize" value = "on"/>
	<property name = "packages" value = "edu.*"/>
	<!-- =================================================================== -->
	  
	<!-- INIT -->
	  
	<!-- =================================================================== -->
	<target name = "init">
		<tstamp/>
	</target>
	<!-- =================================================================== -->
	  
	<!-- PREPARE-SRC -->
	  
	<!-- =================================================================== -->
	<target name = "prepare-src" depends = "init">
		<mkdir dir = "${build.src}"/>
		<mkdir dir = "${build.dest}"/>
		<mkdir dir = "${dist.dir}"/>
		<!-- get source code from subversion repository -->
		<exec executable = "cmd.exe" dir = "${batch.files}" os = "${os}">
			<arg line = "/c checkout ${svn.core.url}/${project.name}/trunk/src ../${project.name}/build/src"/>
		</exec>
		<exec executable = "cmd.exe" dir = "${batch.files}" os = "${os}">
			<arg line = "/c checkout ${svn.core.url}/${project.name}/trunk/lib ../${project.name}/lib"/>
		</exec>
		<exec executable = "cmd.exe" dir = "${batch.files}" os = "${os}">
			<arg line = "/c checkout ${svn.core.url}/${project.name}/trunk/bin ../${project.name}/bin"/>
		</exec>
		<exec executable = "cmd.exe" dir = "${batch.files}" os = "${os}">
			<arg line = "/c checkout ${svn.core.url}/${project.name}/trunk/examples ../${project.name}/examples"/>
		</exec>
		<exec executable = "cmd.exe" dir = "${batch.files}" os = "${os}">
			<arg line = "/c checkout ${svn.core.url}/${project.name}/trunk ../${project.name} --non-recursive"/>
		</exec>
		<!-- add license text to top of source files -->
		<exec executable = "cmd.exe" dir = "${batch.files}" os = "${os}">
			<arg line = "/c merge_license_with_source ../${project.name}/build/src"/>
		</exec>
		<!-- increment the build number -->
		<propertyfile file = "./build/src/edu/stanford/smi/protege/resource/files/build.properties">
			<entry key = "build.number" default = "01" type = "int" operation = "+" pattern = "000"/>
			<entry key = "build.number.previous" default = "00" type = "int" operation = "+" pattern = "000"/>
		</propertyfile>
		<exec executable = "cmd.exe" dir = "${batch.files}" os = "${os}">
			<arg line = "/c commit modified-by-automatic-build ../${project.name}/build/src/edu/stanford/smi/protege/resource/files/build.properties"/>
		</exec>
	</target>
	<!-- =================================================================== -->
	  
	<!-- JAVACC -->
	  
	<!-- =================================================================== -->
	<target name = "javacc" depends = "prepare-src">
		<!-- delete these files because ant's javacc task won't regenerate files when the date on the files is the same as the date on the grammer file. -->
		<delete file = "${build.src}/edu/stanford/smi/protege/storage/clips/ParseException.java"/>
		<delete file = "${build.src}/edu/stanford/smi/protege/storage/clips/Parser.java"/>
		<delete file = "${build.src}/edu/stanford/smi/protege/storage/clips/ParserConstants.java"/>
		<delete file = "${build.src}/edu/stanford/smi/protege/storage/clips/ParserTokenManager.java"/>
		<delete file = "${build.src}/edu/stanford/smi/protege/storage/clips/SimpleCharStream.java"/>
		<delete file = "${build.src}/edu/stanford/smi/protege/storage/clips/Token.java"/>
		<delete file = "${build.src}/edu/stanford/smi/protege/storage/clips/TokenMgrError.java"/>
		<!-- run javacc on parser.jj to regenerate files that were deleted in above step -->
		<javacc javacchome = "c:/javacc-3.2/bin/lib" target = "${build.scripts}/Parser.jj" outputdirectory = "${build.src}/edu/stanford/smi/protege/storage/clips"/>
		<!-- check in files that were regenerated -->
		<exec executable = "cmd.exe" dir = "${batch.files}" os = "${os}">
			<arg line = "/c commit modified-by-automatic-build ../${project.name}/build/src/edu/stanford/smi/protege/storage/clips/ParseException.java"/>
		</exec>
		<exec executable = "cmd.exe" dir = "${batch.files}" os = "${os}">
			<arg line = "/c commit modified-by-automatic-build ../${project.name}/build/src/edu/stanford/smi/protege/storage/clips/Parser.java"/>
		</exec>
		<exec executable = "cmd.exe" dir = "${batch.files}" os = "${os}">
			<arg line = "/c commit modified-by-automatic-build ../${project.name}/build/src/edu/stanford/smi/protege/storage/clips/ParserConstants.java"/>
		</exec>
		<exec executable = "cmd.exe" dir = "${batch.files}" os = "${os}">
			<arg line = "/c commit modified-by-automatic-build ../${project.name}/build/src/edu/stanford/smi/protege/storage/clips/ParserTokenManager.java"/>
		</exec>
		<exec executable = "cmd.exe" dir = "${batch.files}" os = "${os}">
			<arg line = "/c commit modified-by-automatic-build ../${project.name}/build/src/edu/stanford/smi/protege/storage/clips/SimpleCharStream.java"/>
		</exec>
		<exec executable = "cmd.exe" dir = "${batch.files}" os = "${os}">
			<arg line = "/c commit modified-by-automatic-build ../${project.name}/build/src/edu/stanford/smi/protege/storage/clips/Token.java"/>
		</exec>
		<exec executable = "cmd.exe" dir = "${batch.files}" os = "${os}">
			<arg line = "/c commit modified-by-automatic-build ../${project.name}/build/src/edu/stanford/smi/protege/storage/clips/TokenMgrError.java"/>
		</exec>
	</target>
	<!-- =================================================================== -->
	
	<!-- COMPILE -->
	
	<!-- =================================================================== -->
	<target name = "compile" depends = "javacc">
		<javac srcdir = "${build.src}" destdir = "${build.dest}" deprecation = "${deprecation}" debug = "${debug}" optimize = "${optimize}" includeAntRuntime = "${includeAntRuntime}" source = "1.4">
			<classpath>
				<fileset dir = "${lib.dir}">
					<include name = "**/*.jar"/>
				</fileset>
			</classpath>
		</javac>
		<rmic base = "${build.dest}" stubversion = "1.2" classname = "edu.stanford.smi.protege.server.Server"/>
		<rmic base = "${build.dest}" stubversion = "1.2" classname = "edu.stanford.smi.protege.server.ServerProject"/>
		<rmic base = "${build.dest}" stubversion = "1.2" classname = "edu.stanford.smi.protege.server.ServerFrameStore"/>
	</target>
	<!-- =================================================================== -->
	
	<!-- JAR -->
	
	<!-- Create the class package. -->
	
	<!-- =================================================================== -->
	<target name = "jar" depends = "compile">
		<copy todir = "${build.dest}/edu/stanford/smi/protege/resource/files">
			<fileset dir = "${build.src}/edu/stanford/smi/protege/resource/files"/>
		</copy>
		<copy todir = "${build.dest}/edu/stanford/smi/protege/resource/image">
			<fileset dir = "${build.src}/edu/stanford/smi/protege/resource/image"/>
		</copy>
		<jar jarfile = "${dist.dir}/protege.jar" basedir = "${build.dest}" manifest = "${build.src}/meta-inf/manifest.mf"/>
	</target>
	<!-- =================================================================== -->
	
	<!-- JAVADOC -->
	
	<!-- Create the API documentation. -->
	
	<!-- =================================================================== -->
	<target name = "javadoc" depends = "prepare-src">
		<mkdir dir = "${build.javadoc}"/>
		<javadoc packagenames = "${packages}" sourcepath = "${build.src}" destdir = "${build.javadoc}" doctitle = "${Name}" header = "${Name}" footer = "${Name}" windowtitle = "${Name}" author = "true" source = "1.4">
			<bottom><![CDATA[
					<small>
					<a href=mailto:protege-help@smi.stanford.edu>Submit a bug report or feature request</a><br>
					${trademark}<br>
					${copyright}
					</small>]]></bottom>
			<link href = "http://java.sun.com/j2se/1.4/docs/api/"/>
			<link href = "http://www.junit.org/junit/javadoc/3.8/"/>
			<classpath>
				<pathelement location = "${lib.dir}/junit.jar"/>
				<pathelement location = "${lib.dir}/looks.jar"/>
				<pathelement location = "${lib.dir}/unicode_panel.jar"/>
			</classpath>
		</javadoc>
	</target>
	<!-- =================================================================== -->
	
	<!-- DIST -->
	
	<!-- Zips up all source code, resources, and api documentation. -->
	
	<!-- =================================================================== -->
	<target name = "dist" depends = "init">
		<zip zipfile = "${dist.dir}/protege-src-${version}.zip" update = "no">
			<zipfileset dir = "${build.src}" prefix = "${Name}/src"/>
			<zipfileset dir = "${build.javadoc}" prefix = "${Name}/docs/apidocs"/>
			<zipfileset dir = "." includes = "Parser.jj,build.xml,local.properties.template,readme.html" prefix = "${Name}"/>
			<zipfileset dir = "${build.scripts}" includes = "Parser.jj" prefix = "${Name}/bin"/>
			<zipfileset dir = "${lib.dir}" includes = "*.jar" prefix = "${Name}/lib"/>
		</zip>
		<zip zipfile = "${dist.dir}/protege-bin-${version}.zip" update = "no">
			<zipfileset dir = "${dist.dir}" includes = "protege.jar"/>
		</zip>
	</target>
	<!-- =================================================================== -->
	
	<!-- CLEAN -->
	
	<!-- Cleans all generated files and directories. -->
	
	<!-- =================================================================== -->
	<target name = "clean" depends = "init">
		<delete dir = "${build.dir}"/>
		<delete dir = "${build.javadoc}"/>
		<delete dir = "${dist.dir}"/>
	</target>
	<!-- =================================================================== -->
	
	<!-- ALL -->
	
	<!-- Create the jar file, javadoc api documentaion, and the zip file. -->
	
	<!-- =================================================================== -->
	<target name = "all" depends = "jar,javadoc,dist"/>
</project>