package edu.stanford.smi.protege.model;

/**
 * A "Simple Instance" is an instance which is not a class, a slot, or a facet. This interface is just a tag 
 * (it has no methods) to make it easier to test if an instance is simple or not. 
 *
 * @author    Ray Fergerson <fergerson@smi.stanford.edu>
 */
public interface SimpleInstance extends Instance {

}
